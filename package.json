{
  "name": "aa-db",
  "version": "1.1.1",
  "description": "Crawler to drain AA Questions Database",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "dist": "npm run clean && babel src --out-dir dist",
    "sync": "rimraf db && mkdir db && node ./dist/sync.js",
    "postinstall": "npm run sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "git@github.com:alanrsoares/aa-db.git",
  "author": "Alan Soares <alanrsoars@gmail.com> github.com/alanrsoares",
  "license": "MIT",
  "devDependencies": {
    "@types/lowdb": "^1.0.9",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-polyfill": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "isomorphic-fetch": "^3.0.0",
    "lowdb": "^1.0.0"
  }
}
